[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "streamloom"
version = "0.1.0"
description = "A declarative map/filter/reduce pipeline for JSONL streams"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
include = ["streamloom*"]
